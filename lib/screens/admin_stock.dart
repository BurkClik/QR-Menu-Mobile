import 'package:flutter/material.dart';

class AdminStock extends StatelessWidget {
  static String routeName = '/AdminStock';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ADMIN STOCK'),
    );
  }
}
